<% layout(_view.get('_layout/frame/list')) %>

<% block('tab', 'Data history') %>
<% block('title', owner.class.title) %>
<% block('titleTranslation', 'meta') %>

<%- include(_view.get('_part/list/grid'), {
    data: {
        columns: [{
            name: 'createdAt',
            format: 'timestamp'
        },{
            name: 'data'
        },{
            name: 'user',
            format: 'relation'
        }],
        order: {[model.PK]: -1},
        list: _url('list', {c: owner.class.id, id: owner.getId()}),
        jam: 'FrameList',
        create: null,
        update: null,
        delete: null,
        showTop: false
    }
}) %>