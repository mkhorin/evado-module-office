<!-- office/model/select -->
<%
layout(_view.get('_layout/frame/select'));
block('tab', 'Select');
block('title', meta.class.title);
block('titleTranslation', 'meta');
%>

<%- include(_view.get('_meta/list/grid'), {
    data: {
        jam: 'SelectList',
        list: `office/model/list-select?m=${meta.master.attr.name}.${meta.master.model.getMetaId()}`,
        multiple: meta.master.attr.relation.multiple,
        create: null,
        delete: null
    }
}) %>